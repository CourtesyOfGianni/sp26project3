# sp26project3

heapRank: O(n + k log n) time, O(1) auxiliary space
quickSelectRank: O(n) average time, O(log n) auxiliary space (recursion stack)
replaceMin: O(log n) time
rankIncoming: O(n log k) time where k is reporting_interval